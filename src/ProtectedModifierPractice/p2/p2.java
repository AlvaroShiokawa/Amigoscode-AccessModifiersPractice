package ProtectedModifierPractice.p2;

//import ProtectedModifierPractice.p1.P1;

// PRotected classes can only be accsed from the same class, by subclasses of its own, or by other classes in the same
// package as it is.
// P1 and P2 classes are from different packages, respectively p1 and p2 packages, therefore we cannot access
// P1 class´s protected method display() from inside the P2 class.
class p2 {
    public static void main(String[] args) {
//        P1 p1Obj = new P1();
//        p1Obj.display();
    }
}
